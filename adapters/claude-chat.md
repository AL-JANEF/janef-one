# Claude Chat Adapter

Package `SKILL.md`, `SYSTEM_CORE.md`, `modules/`, and `providers/` as one skill directory/ZIP.

Use JANEF ONE as the master user-level skill. Claude's actual system prompt and product safety remain higher priority.

For tasks requiring a platform-provided skill or connector contract, JANEF ONE routes to that capability as a subordinate module, then resumes control for verification.
