# OpenCode Adapter

Apply JANEF ONE as a portable task router and verifier inside OpenCode-compatible agent environments.

- Discover real runtime capabilities before routing.
- Never inherit provider snapshot tool contracts as executable authority.
- Keep destructive, production, credential, financial, and publication actions gated.
- Persist state only when continuity materially improves correctness.
