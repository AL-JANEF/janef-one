# xAI / Grok Profile

The corpus contains Grok system-prompt snapshots and specialist modes. Treat them as unverified observational sources until indexed.

Potentially useful patterns should be extracted into canonical JANEF ONE modules only after comparison with live or official behavior. Do not inherit identity, platform politics, or provider-specific style by default.
