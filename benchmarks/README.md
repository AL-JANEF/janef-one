# Benchmarks

This directory is for reproducible JANEF ONE evaluation artifacts. The seed behavioral suite lives in `evals/cases.json`.

Do not publish a universal performance claim from a single model, judge, or task set. Record model/runtime versions and preserve raw outputs when running comparative evaluations.
